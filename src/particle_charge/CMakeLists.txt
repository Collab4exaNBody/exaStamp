# ======================================================
# === micro-stamp charged particle operator library  ===
# ======================================================

set(exaStampParticleCharge_LINK_LIBRARIES exaStampParticleSpecies)
xstamp_add_plugin(exaStampParticleCharge ${CMAKE_CURRENT_SOURCE_DIR})

